import CategoryList from "./CategoryList";
import SearchBar from "./SearchBar";

const Navigator = () => {
	return (
		<div>
			<CategoryList />
			<SearchBar />
		</div>
	);
};

export default Navigator;
